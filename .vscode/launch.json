{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "extensionHost",
      "request": "launch",
      "name": "Launch CodeShare Extension",
      "args": [
        "--extensionDevelopmentPath=${workspaceFolder}/extension"
      ],
      "preLaunchTask": "npm: compile (extension)",
      "outFiles": ["${workspaceFolder}/extension/out/**/*.js"]
    }
  ]
}

